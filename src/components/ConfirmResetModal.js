const ConfirmResetModal = ({active}) => {
    return (
        <div className={`modal-base ${active ? 'active' : ''}`}>
            <div className="confirm-reset-window">
                
            </div>
        </div>
    )
}

export default ConfirmResetModal
